server:
  address: "%s"

node_config:
  partition-id: %d
  num-partitions: %d

run_config:
  local-epochs: %d
  batch-size: %d
  learning-rate: %f

urls:
  metric-server-url: "%s"

paths:
  dataset-dir: "%s"
  model-file: "%s"
